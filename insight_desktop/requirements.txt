# Insight desktop app dependencies.
# The repo's main .venv (see ../poc/requirements.txt) already has
# llama-cpp-python and huggingface_hub; this adds the desktop-app-specific
# pieces. Install into the same .venv:
#   source ../.venv/bin/activate
#   pip install -r requirements.txt

PySide6>=6.11
llama-cpp-python>=0.3
piper-tts>=1.4
sounddevice>=0.5
soundfile>=0.14
PyYAML>=6.0
numpy>=2.0
